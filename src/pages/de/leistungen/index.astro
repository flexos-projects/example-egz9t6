```astro
---
import { getCollection } from 'astro:content';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Icon from 'astro-icon/components';

// Fetch German services from the content collection
const servicesDe = await getCollection('services', ({ id }) => id.startsWith('de/'));

const page = {
  title: "Unsere Leistungen: Individuelle Web-Apps, Strategie, Cloud | InnovationHub Solutions",
  description: "Entdecken Sie unsere B2B IT-Dienstleistungen, einschließlich der Entwicklung individueller Webanwendungen, digitaler Strategieberatung, Cloud-Migration und E-Commerce-Lösungen für den bayerischen Mittelstand.",
  lang: 'de',
};
---
<BaseLayout title={page.title} description={page.description} lang={page.lang}>
  
  <!-- Minimal Hero (Pattern 2.3) -->
  <section class="py-24 md:py-32 lg:py-40 bg-surface-50 text-center">
    <div class="mx-auto max-w-7xl px-4 md:px-8 lg:px-12">
      <div class="max-w-3xl mx-auto opacity-0 translate-y-6 motion-safe:animate-[fade-in-up_500ms_ease-out_forwards]">
        <h1 class="font-inter text-4xl md:text-5xl lg:text-6xl font-semibold leading-tight text-primary-700 mb-4">
          Unsere Leistungen
        </h1>
        <p class="font-lora text-lg md:text-xl text-surface-900 opacity-90 mb-8">
          Wir übersetzen komplexe technologische Herausforderungen in klare, wirkungsvolle Geschäftsstrategien. Entdecken Sie unser Leistungsspektrum, das darauf ausgerichtet ist, nachhaltiges Wachstum für den bayerischen Mittelstand zu fördern.
        </p>
      </div>
    </div>
  </section>

  <!-- Services Grid Section -->
  <section class="py-16 md:py-24 lg:py-32 bg-surface-100">
    <div class="mx-auto max-w-7xl px-4 md:px-8 lg:px-12">
      <div class="grid md:grid-cols-2 gap-8 max-w-4xl mx-auto">
        {servicesDe.map((service, index) => (
          <a 
            href={`/de/leistungen/${service.slug.replace('de/', '')}`}
            class="group bg-surface-50 rounded-md shadow-md p-8 transition-all duration-300 ease-in-out hover:shadow-xl hover:-translate-y-1 block opacity-0 translate-y-6 motion-safe:animate-[fade-in-up_500ms_ease-out_forwards]"
            style={`--delay: ${index * 150}ms; animation-delay: ${index * 150}ms;`}
          >
            <div class="flex items-start mb-4">
              <div class="bg-primary-50 text-primary-600 rounded-md p-3 mr-5 flex-shrink-0">
                <Icon name={service.data.icon || 'lucide:box'} class="w-8 h-8 transition-transform duration-300 ease-in-out group-hover:scale-110" />
              </div>
              <div>
                <h3 class="font-inter text-2xl font-semibold text-primary-700 mb-2">
                  {service.data.title}
                </h3>
              </div>
            </div>
            <p class="font-lora text-base text-surface-900 mb-6 leading-relaxed">
              {service.data.summary}
            </p>
            <span class="font-inter text-accent-500 group-hover:text-accent-600 font-medium text-sm transition-colors duration-300 ease-in-out">
              Mehr erfahren &rarr;
            </span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Banner (Pattern 3.5 adapted in a Prominent Background Section - Pattern 1.3) -->
  <section class="py-20 md:py-28 bg-primary-700 text-white">
    <div class="mx-auto max-w-7xl px-4 md:px-8 lg:px-12 text-center">
      <div class="max-w-4xl mx-auto opacity-0 translate-y-6 motion-safe:animate-[fade-in-up_500ms_ease-out_forwards]">
        <h2 class="font-inter text-3xl md:text-4xl font-bold leading-tight mb-4">
          Haben Sie ein Projekt im Sinn?
        </h2>
        <p class="font-lora text-lg md:text-xl mb-8 opacity-90 max-w-2xl mx-auto">
          Lassen Sie uns darüber sprechen, wie unsere maßgeschneiderten Lösungen Ihr Unternehmen voranbringen können. Wir freuen uns darauf, von Ihrer Vision zu hören.
        </p>
        <a href="/de/kontakt" class="inline-flex items-center bg-accent-500 hover:bg-accent-600 text-primary-950 font-inter font-medium py-3 px-6 rounded-md shadow-lg transition-all duration-300 ease-in-out hover:shadow-xl hover:-translate-y-0.5">
          Kostenloses Beratungsgespräch
          <Icon name="lucide:arrow-right" class="ml-2 w-4 h-4" />
        </a>
      </div>
    </div>
  </section>

</BaseLayout>
```